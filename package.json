{
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "dev": "concurrently \"npm start\" \"cd ../backend && mvnw.cmd spring-boot:run\""
  },
  "dependencies": {
    "react-scripts": "^5.0.1"
  }
}
